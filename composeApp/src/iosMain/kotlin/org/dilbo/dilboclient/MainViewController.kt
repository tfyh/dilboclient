package org.dilbo.dilboclient

import androidx.compose.ui.window.ComposeUIViewController

// TODO uncomment the following line for iOS compilation
// fun MainViewController() = ComposeUIViewController { App(HttpClient(Darwin.create() {asf.}) } )